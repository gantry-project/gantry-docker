import React, { FC } from "react";
import styled from "styled-components";

const DockerItemCard = () => {
  return <Container>card</Container>;
};

export default DockerItemCard;

const Container = styled.div``;
