import React from "react";
import styled from "styled-components";

//component

const DockerListHeader = () => {
  return <Container>filter랑 검색기능 추가 </Container>;
};

export default DockerListHeader;

const Container = styled.div`
  height: 60px;
`;
